import RegisterPage from "@/components/auth/RegisterPage";
import type { Metadata } from "next";
export const metadata: Metadata = { title: "নিবন্ধন" };
export default function Register() { return <RegisterPage />; }
